// Используйте IntelliSense, чтобы узнать о возможных атрибутах.
    // Наведите курсор на существующие атрибуты, чтобы просмотреть их описания.
    // Для получения дополнительной информации посетите: https://go.microsoft.com/fwlink/?linkid=830387
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            // ⭐️ УКАЖИТЕ ПРАВИЛЬНЫЙ ПУТЬ К ВАШЕМУ ТЕСТОВОМУ ИСПОЛНЯЕМОМУ ФАЙЛУ
            // Замените 'имя_вашего_теста' на фактическое имя (например, 'FourTest')
            "program": "${workspaceFolder}/build/four.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb", // Или "lldb" для macOS
            "setupCommands": [
                {
                    "description": "Enable pretty printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            // ⭐️ УКАЖИТЕ ПРАВИЛЬНЫЙ ПУТЬ К GDB/LLDB, если он не находится в PATH
            "miDebuggerPath": "C:/msys64/mingw64/bin/gdb.exe", 
            
            // 💡 ВАЖНО: Убедитесь, что отладка запускается после успешной сборки.
            "preLaunchTask": "cmake_build" 
        }
    ]
}
